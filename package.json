{
  "name": "render-css-test",
  "version": "1.0.0",
  "description": "A minimal test to check static file serving on Render.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
